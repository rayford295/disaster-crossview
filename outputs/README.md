This folder stores generated outputs such as maps, figures, HTML visualizations, and other reports.

These outputs are intentionally ignored by Git unless you later choose to version a specific lightweight example.
