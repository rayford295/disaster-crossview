This folder is reserved for the local unlabeled Altadena imagery workflow.

Keep large imagery, downloaded attachment indexes, and generated matching outputs here locally, but do not commit those data files to GitHub.

Files that are meant to stay versioned:

- `download_attachment_index.py`
- `match_remote_sensing.py`
- `requirements.txt`

Folders preserved in Git with `.gitkeep`:

- `Altadena_Images/`
- `Eaton_Fire_attachments_index_output/dataset/`
