This folder stores generated project artifacts such as parsed metadata tables, GeoJSON exports, feature arrays, and train/validation/test split files.

These outputs are intentionally ignored by Git. Keep them locally and regenerate them when needed.
